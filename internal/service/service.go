package service

package service

import (
    "errors"
    "strings"
)

// Convert принимает строку и возвращает её в противоположном формате:
// текст -> Морзе, Морзе -> текст.
func Convert(input string) (string, error) {
    // Шаг 1: Базовая проверка на пустую строку
    if input == "" {
        return "", errors.New("входная строка пуста")
    }

    // Шаг 2: Определяем тип строки (текст или Морзе)
    // Подумай: какие символы чаще всего встречаются в Морзе? (., -, пробел)
    // А в обычном тексте? (буквы)
    isMorse := isMorseString(input)

    if isMorse {
        // Шаг 3: Если это Морзе, вызываем функцию перевода в текст
        return fromMorse(input)
    } else {
        // Шаг 4: Если это текст, вызываем функцию перевода в Морзе
        return toMorse(input)
    }
}

// isMorseString помогает понять, что нам передали: Морзе или обычный текст.
// Верни true, если похоже на Морзе, и false, если похоже на текст.
func isMorseString(s string) bool {
    // Подсказка: можно проверить, есть ли в строке точки и тире,
    // и при этом нет ли там букв (или их очень мало).
    // Попробуй использовать strings.Contains или strings.IndexAny.
    return false // Замени это на свою логику!
}

// toMorse переводит обычный текст в код Морзе.
func toMorse(text string) (string, error) {
    // 1. Приведи текст к одному регистру (например, strings.ToUpper),
    //    чтобы 'a' и 'A' обрабатывались одинаково.
    // 2. Пройдись по каждой букве.
    // 3. Найди её в словаре (map) и добавь соответствующий код.
    // 4. Разделяй буквы пробелами, а слова — двойным пробелом (или как требуется в ТЗ).
    return "", nil
}

// fromMorse переводит код Морзе в обычный текст.
func fromMorse(code string) (string, error) {
    // 1. Раздели строку на части по пробелам (strings.Split).
    //    Получится срез строк, где каждый элемент — это код одной буквы.
    // 2. Пройдись по этому срезу.
    // 3. Найди соответствие в обратном словаре.
    // 4. Собери буквы обратно в слово (strings.Join).
    return "", nil
}